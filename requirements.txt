# Data stack for the flagship (install into .venv).
# Airflow itself lives in a separate orchestrator venv (see Makefile: airflow).
pandas>=2.0
numpy>=1.26
pyarrow>=15.0
duckdb>=1.0
dbt-duckdb>=1.7
pyspark>=3.5
