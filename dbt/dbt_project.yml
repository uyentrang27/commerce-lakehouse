name: 'commerce_lakehouse'
version: '1.0.0'
config-version: 2

profile: 'commerce_lakehouse'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

models:
  commerce_lakehouse:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: gold

snapshots:
  commerce_lakehouse:
    +target_schema: snapshots
